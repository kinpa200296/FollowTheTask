@model IEnumerable<FollowTheTask.BLL.Services.Comment.ViewModels.CommentInfoViewModel>

@if (Model != null && Model.Count() != 0)
{
    foreach (var item in Model)
    {
        @Html.Action("Index", "Comment", new {model = item})
    }
}
